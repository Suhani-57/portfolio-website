# ---------- OS ----------
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# ---------- Editors / IDEs ----------
.vscode/
.idea/
*.swp
*.swo
*~

# ---------- Claude Code ----------
# Shared project tooling stays tracked (.claude/launch.json, .claude/serve.ps1).
# Only per-machine local settings are ignored.
.claude/settings.local.json
.claude/*.local.json

# ---------- Temporary / scratch files ----------
__tmp_*
*.tmp
*.bak
*.log

# ---------- Environment ----------
.env
.env.*
!.env.example

# ---------- Node (only if build tooling is added later) ----------
node_modules/
npm-debug.log*
yarn-error.log*
